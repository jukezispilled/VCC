/*
const User = require('../models/User');

// Example of adding an admin user
const adminUser = new User({
  name: 'Admin User',
  email: 'admin@example.com',
  password: 'adminpassword',
  role: 'admin',
});

adminUser.save()
  .then(() => {
    console.log('Admin user added successfully');
  })
  .catch((error) => {
    console.error('Failed to add admin user:', error);
  });

// Example of adding a general user
const generalUser = new User({
  name: 'General User',
  email: 'user@example.com',
  password: 'userpassword',
});

generalUser.save()
  .then(() => {
    console.log('General user added successfully');
  })
  .catch((error) => {
    console.error('Failed to add general user:', error);
  });
  */
